<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity_sell.DesctiptionETActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_alignParentTop="true"
        android:background="?attr/colorPrimary"
        app:theme="@style/AppTheme.ToolbarLikeDialog">

        <CustomComponents.TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Tulis Deskripsi Produkmu..." />

    </android.support.v7.widget.Toolbar>

    <CustomComponents.EditText
        android:layout_below="@id/toolbar"
        android:id="@+id/mDescription"
        android:padding="@dimen/padding_normal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="Tulis deskripsimu..."
        android:inputType="textMultiLine"
        android:singleLine="false"
        android:gravity="top|left"
        android:background="@drawable/edittext_fullscreen"/>
</RelativeLayout>
